// @flow
import {SEARCH_PLAYLIST} from '../actions/playlist';

const DEFAULT = {};

export default function counter(state: Object = DEFAULT, action: Object) {
  switch (action.type) {
      return state;
  }
}
